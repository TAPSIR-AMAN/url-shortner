<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Forgot Password</title>

  <link rel="stylesheet" href="/style.css" />
</head>

<body>
    <%- include('../partial/header') %>

<div class="forgot-password-page">

  <!-- 🔥 ERROR MESSAGES -->
  
  
  <!-- 🔥 SUCCESS AFTER FORM SUBMISSION -->
  <% if (formSubmitted) { %>
    <p class="success-message">
        If the email that you have given us is correct,  
        we have sent an email to reset your password.
    </p>
    
    <% } else { %>
        
        <!-- 🔥 RESET PASSWORD FORM -->
        <div class="change-password-form">
            <h1 class="form-title">Enter Your Register Email</h1>
            
            <form action="/reset-password" method="post">
                
                <div class="form-group">
                    <label for="email"></label>
                    
                    <div class="input-wrapper">
                        <i class="zmdi zmdi-email prefix-icon"></i>
                        
                        <input
                        type="text"
                        name="email"
                        id="email"
                        placeholder="Enter your Register Email"
                        autocomplete="false"
                        
                        />
                    </div>
                </div>
                
                <% if (errors && errors.length > 0) { %>
                  <div class="error-container">
                    <% errors.forEach((error) => { %>
                      <p class="flash-error"><%= error %></p>
                    <% }) %>
                  </div>
                <% } %>
                <div class="form-group form-button">
                    <button type="submit" class="form-submit">Submit</button>
                </div>

      </form>
    </div>

  <% } %>

</div>

<%- include('../partial/footer') %>
</body>
</html>
